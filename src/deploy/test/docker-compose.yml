version: "3.3"
services:
  tests:
    build: ../../..
    volumes:
      - ./test-run.sh:/src/test-run.sh
    environment:
      - SECRET_KEY=insecure
      - DEBUG=True
    command: /src/test-run.sh
