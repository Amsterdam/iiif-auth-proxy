version: "3.3"
services:
  tests:
    build: ../../..
    volumes:
#      - ./app:/app
      - ./test-run.sh:/app/test-run.sh
#    ports:
#      - "8000:8000"
    environment:
      - SECRET_KEY=insecure
      - DEBUG=True
    command: /app/test-run.sh
